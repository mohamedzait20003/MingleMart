import AudienceLayout from "@/common/layouts/AudienceLayout"

/**
 * A dead end keeps the bar the reader arrived with.
 *
 * Stripping the navigation is what turns a wrong URL into a trap: the page that
 * says "we cannot find that" is exactly the page that most needs a way out.
 */
export default AudienceLayout
