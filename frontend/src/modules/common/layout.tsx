import AudienceLayout from "@/common/layouts/AudienceLayout"

/**
 * The company pages: about, careers, privacy, terms.
 *
 * One URL per page, whoever is reading, and no navigation of its own - the bar
 * is the one the reader already had. A company-specific bar would be a fourth
 * set of links that belongs to nobody, and following one would strand a
 * customer outside their own shell.
 */
export default AudienceLayout
