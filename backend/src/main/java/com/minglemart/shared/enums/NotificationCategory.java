package com.minglemart.shared.enums;

/** Mirrors the CHECK constraint on {@code notifications.category}. */
public enum NotificationCategory {
    SECURITY,
    ACCOUNT,
    ORDER,
    SHIPPING,
    PAYMENT,
    MARKETING,
    AGENT
}
